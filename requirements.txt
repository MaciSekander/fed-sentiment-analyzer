requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
pandas>=2.0
matplotlib>=3.7
python-dateutil>=2.8
tqdm>=4.66

# Optional, only needed for the transformer-based scorer (src/sentiment/model.py).
# The lexicon baseline and CLI work without these.
transformers>=4.35
torch>=2.0

pytest>=7.4
